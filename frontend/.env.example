# Used by vite.config.ts as the proxy target during `npm run dev`.
# In dev: leave as http://localhost:8000.
# In Docker: nginx serves the build, so this is only used at dev time.
VITE_API_BASE_URL=http://localhost:8000